PORT=3001
CORS_ORIGIN=http://localhost:5173
DATABASE_URL=postgres://workhub:workhub@localhost:5432/workhub?sslmode=disable

# Required in production: clients must send this value in the X-API-Key header.
# If unset, API authentication is disabled (local development only).
API_KEY=changeme-generate-a-long-random-value
